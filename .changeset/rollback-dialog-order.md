---
"@auriclabs/migrations": patch
---

Fix Rollback dialog ordering: list completed migrations newest-first so the most recent rollback target appears at the top of the list, and correct the "Roll back N migrations down to X" count to match the displayed order.
